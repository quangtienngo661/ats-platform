/**
 * @deprecated This component is no longer used.
 * Candidate management is now accessed through /jobs/[jobId]/candidates
 * using CandidatesFilters, CandidatesHeader, and CandidatesStats components directly.
 */
export default function CandidatesClient() {
    return null;
}
