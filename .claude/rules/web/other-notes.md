---
paths:
  - "apps/web/**"
---

# Web — Other Notes

- Repo-root `CLAUDE.md` covers monorepo-wide conventions (Prisma location, API response envelope, BullMQ queues) — don't duplicate those here.
- `apps/api/CLAUDE.md` documents the backend this frontend talks to exclusively through `src/servers/*`.
